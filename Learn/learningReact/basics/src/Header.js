const header = () => {
    const headerStyle = {
        backgroundColor:'grey',
        color: '#fff'
    }
    return (
        <header style = {headerStyle}>
            <h1>Groceries List</h1>
        </header>
    )
}

export default header;